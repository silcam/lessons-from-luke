// import { Express } from "express";
// import { readSourceManifest, readSource } from "../FileStorage";
// import { addGetHandler } from "../api/WebAPI";

// export default function sourcesController(app: Express) {
//   addGetHandler(app, "/api/sources", async req => {
//     return readSourceManifest();
//   });

//   addGetHandler(app, "/api/sources/:language", async req => {
//     return readSource(req.params.language);
//   });
// }
