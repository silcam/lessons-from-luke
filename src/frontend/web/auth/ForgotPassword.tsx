/**
 * ForgotPassword.tsx — Self-service "forgot password" request page
 *
 * Route: /forgot-password
 * Spec: specs/005-transactional-email-reset/spec.md §US1
 * Plan: plan.md §Presentation Design (UI Decisions), §Accessibility Requirements
 *
 * RED STUB — renders null. Tests in ForgotPassword.test.tsx describe the required
 * behavior. The GREEN task (lessons-from-luke-5qjl.5.3.6) implements this fully.
 */

import React from "react";

export default function ForgotPassword(): React.ReactElement | null {
  return null;
}
