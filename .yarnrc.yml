nodeLinker: node-modules

# Allow build scripts to run (needed for libxmljs2, electron, cypress).
enableScripts: true

# Local installs stay mutable; CI/deploy pass --immutable explicitly.
enableImmutableInstalls: false

yarnPath: .yarn/releases/yarn-4.14.1.cjs
